<UserControl x:Class="OBSArrastre2026.App.Controls.DatePickerPremium"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200">
    <Grid>
        <Border x:Name="MainBorder"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1.5"
                CornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox x:Name="DateInput"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="12,8"
                         FontSize="14"
                         Foreground="{DynamicResource PrimaryTextBrush}"
                         CaretBrush="{DynamicResource AccentBrush}"
                         VerticalContentAlignment="Center"
                         TextChanged="DateInput_TextChanged"
                         PreviewKeyDown="DateInput_PreviewKeyDown"
                         LostFocus="DateInput_LostFocus"/>

                <ToggleButton x:Name="CalendarButton"
                              Grid.Column="1"
                              Width="38"
                              Background="Transparent"
                              BorderThickness="0"
                              Cursor="Hand"
                              IsChecked="{Binding IsOpen, ElementName=CalendarPopup, Mode=TwoWay}">
                    <TextBlock Text="📅" 
                               FontSize="16" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>
                </ToggleButton>
            </Grid>
        </Border>

        <Popup x:Name="CalendarPopup"
               PlacementTarget="{Binding ElementName=MainBorder}"
               Placement="Bottom"
               StaysOpen="False"
               AllowsTransparency="True"
               PopupAnimation="Fade">
            <Border Margin="0,4,0,0"
                    Background="{DynamicResource CardBackgroundBrush}"
                    BorderBrush="{DynamicResource CardBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Direction="270" Opacity="0.15" ShadowDepth="5" Color="Black"/>
                </Border.Effect>
                <Calendar x:Name="CalendarControl"
                          Margin="8"
                          SelectedDate="{Binding SelectedDate, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                          DisplayDate="{Binding SelectedDate, RelativeSource={RelativeSource AncestorType=UserControl}, TargetNullValue={x:Static System:DateTime.Today}, Mode=OneWay}"
                          xmlns:System="clr-namespace:System;assembly=mscorlib"/>
            </Border>
        </Popup>
    </Grid>
</UserControl>
