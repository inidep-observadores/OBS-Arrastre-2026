<UserControl x:Class="ControlMareas.App.Views.ConfiguracionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ControlMareas.App.Views"
             xmlns:vm="clr-namespace:ControlMareas.App.ViewModels"
             xmlns:models="clr-namespace:ControlMareas.App.Models"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{DynamicResource HeroBackgroundBrush}">
    
    <UserControl.Resources>
        <Style x:Key="SettingsCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="Padding" Value="32" />
            <Setter Property="Margin" Value="0,0,0,32" />
        </Style>

        <Style x:Key="SettingLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>

        <Style x:Key="SettingDescriptionStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Margin" Value="0,0,0,20" />
            <Setter Property="Opacity" Value="0.8" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="32,40">
                
                <StackPanel Orientation="Horizontal" Margin="0,0,0,40">
                    <Border Width="56" Height="56" CornerRadius="16" Background="{DynamicResource AccentMutedBrush}" Margin="0,0,20,0">
                        <TextBlock Text="⚙" FontSize="28" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Configuración" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" />
                        <TextBlock Text="Personaliza tu experiencia y ajusta las preferencias del revisor." FontSize="15" Foreground="{DynamicResource SecondaryTextBrush}" />
                    </StackPanel>
                </StackPanel>

                <!-- Sección: Revisor -->
                <TextBlock Text="REVISOR" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource SecondaryTextBrush}" Opacity="0.6" Margin="4,0,0,16" />
                
                <Border Style="{StaticResource SettingsCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Nombre del Revisor" Style="{StaticResource SettingLabelStyle}" />
                        <TextBlock Text="Estos datos se guardarán automáticamente al salir del campo de texto." Style="{StaticResource SettingDescriptionStyle}" />
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="24" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel>
                                <TextBlock Text="Nombre" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8" />
                                <TextBox Text="{Binding NombreRevisor}" Style="{StaticResource PremiumTextBoxStyle}" />
                            </StackPanel>
                            
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Apellido" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,8" />
                                <TextBox Text="{Binding ApellidoRevisor}" Style="{StaticResource PremiumTextBoxStyle}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Sección: Apariencia -->
                <TextBlock Text="APARIENCIA" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource SecondaryTextBrush}" Opacity="0.6" Margin="4,0,0,16" />
                
                <Border Style="{StaticResource SettingsCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel>
                            <TextBlock Text="Tema Visual" Style="{StaticResource SettingLabelStyle}" />
                            <TextBlock Text="Selecciona el modo de color que prefieras para la interfaz de la aplicación." Style="{StaticResource SettingDescriptionStyle}" />
                        </StackPanel>
                        
                        <Border Grid.Column="1" 
                                Style="{StaticResource SurfaceBorderStyle}"
                                VerticalAlignment="Top"
                                Padding="6">
                            <StackPanel Orientation="Horizontal">
                                <Button Command="{Binding SetSystemThemeCommand}" Content="Sistema" Width="90" Height="36">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource ThemeButtonStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSystemThemeActive}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource SelectionBackgroundBrush}" />
                                                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Command="{Binding SetLightThemeCommand}" Content="Claro" Width="90" Height="36">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource ThemeButtonStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLightThemeActive}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource SelectionBackgroundBrush}" />
                                                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Command="{Binding SetDarkThemeCommand}" Content="Oscuro" Width="90" Height="36">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource ThemeButtonStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDarkThemeActive}" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource SelectionBackgroundBrush}" />
                                                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <!-- Feedback de Guardado Automático -->
                <TextBlock Text="✓ Los cambios se guardan automáticamente." 
                           HorizontalAlignment="Right" 
                           Foreground="{DynamicResource SuccessForegroundBrush}" 
                           FontSize="12" 
                           FontStyle="Italic"
                           Opacity="0.7"
                           Margin="0,0,4,0" />
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
