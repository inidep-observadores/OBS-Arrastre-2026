<Window x:Class="ControlMareas.App.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SplashWindow" Height="400" Width="600"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="False">
    <Border Background="{DynamicResource CardBackgroundBrush}"
            BorderBrush="{DynamicResource CardBorderBrush}"
            BorderThickness="1"
            CornerRadius="0">
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="10" Opacity="0.15" Color="Black"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Accent Bar -->
            <Border Grid.Row="0" Height="4" Background="{DynamicResource AccentBrush}" />

            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="pack://application:,,,/assets/icons/app_icon.png" 
                       Width="120" Height="120" 
                       Margin="0,0,0,24">
                    <Image.Effect>
                        <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2" Color="{DynamicResource AccentColor}"/>
                    </Image.Effect>
                </Image>
                
                <TextBlock Text="Control de mareas" 
                           FontSize="32" 
                           FontWeight="SemiBold" 
                           FontFamily="{DynamicResource DisplayFontFamily}"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           HorizontalAlignment="Center" />
                
                <TextBlock Text="INIDEP - Observadores a Bordo" 
                           FontSize="14" 
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="40,0,40,48">
                <ProgressBar x:Name="ProgressBar" 
                             IsIndeterminate="True" 
                             Height="4" 
                             Background="{DynamicResource AccentMutedBrush}"
                             Foreground="{DynamicResource AccentBrush}"
                             BorderThickness="0"
                             Style="{StaticResource SlimProgressBarStyle}"/>
                
                <TextBlock x:Name="StatusText" 
                           Text="Iniciando aplicación..." 
                           FontSize="13" 
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
